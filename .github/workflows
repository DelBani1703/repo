name: Afficher les fichiers

on: [push]

jobs:
  afficher-fichiers:
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v4

      - name: Afficher les fichiers
        run: ls -la
